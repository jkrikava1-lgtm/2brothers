---
import '../styles/global.css';
import site from '../data/site.json';

interface Props {
  title: string;
  description?: string;
}

const { title, description = site.tagline } = Astro.props;
const currentPath = Astro.url.pathname;
---

<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Outfit:wght@600;700;800;900&display=swap" rel="stylesheet" />
  <link rel="icon" type="image/png" href="/favicon.png" />
</head>
<body class="min-h-screen flex flex-col relative overflow-x-hidden">

  <!-- Global glow orbs -->
  <div class="fixed inset-0 pointer-events-none z-0">
    <div class="absolute top-[10%] left-[10%] w-[500px] h-[500px] bg-pink-500/15 rounded-full blur-[150px] animate-pulse"></div>
    <div class="absolute top-[30%] right-[5%] w-[400px] h-[400px] bg-cyan-400/10 rounded-full blur-[130px] animate-pulse" style="animation-delay: 1s;"></div>
    <div class="absolute top-[55%] left-[30%] w-[350px] h-[350px] bg-purple-500/10 rounded-full blur-[120px] animate-pulse" style="animation-delay: 2s;"></div>
    <div class="absolute top-[75%] right-[20%] w-[450px] h-[450px] bg-pink-500/10 rounded-full blur-[140px] animate-pulse" style="animation-delay: 3s;"></div>
    <div class="absolute top-[90%] left-[5%] w-[300px] h-[300px] bg-cyan-400/8 rounded-full blur-[100px] animate-pulse" style="animation-delay: 4s;"></div>
  </div>

  <!-- Header -->
  <header class="fixed top-0 left-0 right-0 z-50 overflow-hidden">
    <!-- Header glow -->
    <div class="absolute inset-0 bg-night-950"></div>
    <div class="absolute -top-20 -left-20 w-[300px] h-[300px] bg-pink-500/15 rounded-full blur-[100px]"></div>
    <div class="absolute -top-10 right-[20%] w-[300px] h-[300px] bg-pink-500/12 rounded-full blur-[100px]"></div>
    <div class="absolute -top-10 right-[5%] w-[250px] h-[250px] bg-purple-500/10 rounded-full blur-[80px]"></div>
    <div class="absolute -bottom-10 right-0 w-[200px] h-[200px] bg-cyan-400/8 rounded-full blur-[80px]"></div>
    <div class="relative max-w-7xl mx-auto px-4 h-36 lg:h-48 flex items-center justify-between">
      <!-- Logo -->
      <a href="/" class="flex items-center gap-2 group">
        <div class="relative">
          <div class="absolute inset-0 bg-pink-500/25 rounded-full blur-[60px] scale-150"></div>
          <div class="absolute top-1/2 -right-[100%] -translate-y-1/2 w-[400px] h-[200px] bg-pink-500/15 rounded-full blur-[80px]"></div>
          <img src="/images/logo.png" alt="2Brothers Music Factory" class="relative h-32 lg:h-44 w-auto" style="filter: drop-shadow(0 0 6px rgba(255,50,100,0.9)) drop-shadow(0 0 15px rgba(255,50,100,0.7)) drop-shadow(0 0 40px rgba(255,50,100,0.5)) drop-shadow(0 0 80px rgba(255,50,100,0.3)) drop-shadow(0 0 150px rgba(255,50,100,0.2)) drop-shadow(0 0 250px rgba(255,50,100,0.1)) drop-shadow(0 0 400px rgba(255,50,100,0.05)) brightness(1.2);" />
        </div>
      </a>

      <!-- Desktop Nav -->
      <nav class="hidden lg:flex items-center gap-2">
        {site.nav_items.map((item: { label: string; href: string }) => (
          <a
            href={item.href}
            class:list={[
              'px-5 py-2.5 rounded-full text-sm font-bold transition-all border',
              currentPath === item.href || (item.href !== '/' && currentPath.startsWith(item.href))
                ? 'text-white bg-pink-600/30 border-pink-400/60 shadow-[0_0_30px_rgba(255,50,100,0.6),0_0_60px_rgba(255,50,100,0.2)]'
                : 'text-pink-200 bg-pink-500/10 border-pink-500/25 shadow-[0_0_12px_rgba(255,26,126,0.25)] hover:bg-pink-500/20 hover:shadow-[0_0_18px_rgba(255,26,126,0.35)] hover:text-white'
            ]}
          >
            {item.label}
          </a>
        ))}
      </nav>

      <!-- Mobile hamburger -->
      <button
        id="mobile-toggle"
        class="lg:hidden w-10 h-10 flex items-center justify-center text-white/80 hover:text-white"
        aria-label="Otevřít menu"
      >
        <svg id="icon-open" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
        <svg id="icon-close" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>

    <!-- Mobile Nav -->
    <div id="mobile-nav" class="lg:hidden hidden bg-night-950/95 backdrop-blur-xl">
      <nav class="max-w-7xl mx-auto px-4 py-4 flex flex-col gap-1">
        {site.nav_items.map((item: { label: string; href: string }) => (
          <a
            href={item.href}
            class:list={[
              'px-5 py-3 rounded-full text-base font-bold transition-all border',
              currentPath === item.href || (item.href !== '/' && currentPath.startsWith(item.href))
                ? 'text-white bg-pink-600/30 border-pink-400/60 shadow-[0_0_30px_rgba(255,50,100,0.6),0_0_60px_rgba(255,50,100,0.2)]'
                : 'text-pink-200 bg-pink-500/10 border-pink-500/25 shadow-[0_0_12px_rgba(255,26,126,0.25)] hover:bg-pink-500/20 hover:shadow-[0_0_18px_rgba(255,26,126,0.35)] hover:text-white'
            ]}
          >
            {item.label}
          </a>
        ))}
      </nav>
    </div>
  </header>

  <!-- Main -->
  <main class="flex-1 pt-36 lg:pt-48 relative z-10">
    <slot />
  </main>

  <!-- Footer -->
  <footer class="relative z-10 overflow-hidden">
    <div class="relative max-w-7xl mx-auto px-4 py-12">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <!-- Brand -->
        <div>
          <a href="/" class="inline-block relative">
            <div class="absolute inset-0 bg-pink-500/25 rounded-full blur-[40px] scale-150"></div>
            <img src="/images/logo.png" alt="2Brothers Music Factory" class="relative h-24 w-auto mb-3" style="filter: drop-shadow(0 0 6px rgba(255,50,100,0.9)) drop-shadow(0 0 15px rgba(255,50,100,0.7)) drop-shadow(0 0 40px rgba(255,50,100,0.5)) drop-shadow(0 0 80px rgba(255,50,100,0.3)) brightness(1.2);" />
          </a>
          <p class="text-white/50 text-sm">{site.tagline}</p>
          <p class="text-white/40 text-sm mt-2">{site.locations.join(' · ')}</p>
        </div>

        <!-- Contact -->
        <div>
          <h4 class="text-sm font-bold text-pink-400 uppercase tracking-wider mb-3">Kontakt</h4>
          <div class="space-y-1.5 text-sm text-white/50">
            <p>Jiří Samšiňák: <a href={`tel:${site.phone_jiri.replace(/\s/g, '')}`} class="text-white/80 hover:text-pink-400 transition-colors">{site.phone_jiri}</a></p>
            <p>Jakub Křikava: <a href={`tel:${site.phone_jakub.replace(/\s/g, '')}`} class="text-white/80 hover:text-pink-400 transition-colors">{site.phone_jakub}</a></p>
            <p><a href={`mailto:${site.email}`} class="text-white/80 hover:text-pink-400 transition-colors">{site.email}</a></p>
          </div>
        </div>

        <!-- Nav -->
        <div>
          <h4 class="text-sm font-bold text-cyan-400 uppercase tracking-wider mb-3">Navigace</h4>
          <div class="space-y-1.5">
            {site.nav_items.map((item: { label: string; href: string }) => (
              <a href={item.href} class="block text-sm text-white/50 hover:text-pink-400 transition-colors">
                {item.label}
              </a>
            ))}
          </div>
        </div>
      </div>

      <div class="mt-10 pt-6 border-t border-white/10 text-center text-xs text-white/30">
        {site.footer_text}
      </div>
    </div>
  </footer>

  <script>
    const toggle = document.getElementById('mobile-toggle');
    const nav = document.getElementById('mobile-nav');
    const iconOpen = document.getElementById('icon-open');
    const iconClose = document.getElementById('icon-close');

    toggle?.addEventListener('click', () => {
      const isOpen = !nav?.classList.contains('hidden');
      nav?.classList.toggle('hidden');
      iconOpen?.classList.toggle('hidden');
      iconClose?.classList.toggle('hidden');
      toggle.setAttribute('aria-label', isOpen ? 'Otevřít menu' : 'Zavřít menu');
    });
  </script>
</body>
</html>
